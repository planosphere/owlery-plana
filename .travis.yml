services:
  - docker
  
script:
  - docker build -t test-image:test .
  - docker run -d --name test -p localhost:80:9024 test-image:test
  - sleep 2m
  - docker logs test
  - curl -sSf http://localhost/kbs/vfb
